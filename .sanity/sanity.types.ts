/**
 * Stub types for Sanity generated types.
 *
 * This file allows the project to build without a connected Sanity project.
 * Once you run `npm run sanity:typegen` with a real Sanity project,
 * this file will be overwritten with the actual generated types.
 */

// biome-ignore lint/suspicious/noExplicitAny: stub types for build compatibility
type Any = any;

export type PostQueryResult = Any;
export type PostsArchiveQueryResult = { results: Any[]; total: number };
export type SettingsQueryResult = Any;
export type BlogPageQueryResult = Any;
export type GetPageQueryResult = Any;
